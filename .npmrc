; npm 10.8.2 (bundled with Node 20) crashes with
;   "Cannot read properties of null (reading 'edgesOut')"
; while resolving vitest's optional peer set. Skipping automatic peer
; installation avoids that code path. The dependency graph itself is sound:
; installing every other package without this flag resolves cleanly.
legacy-peer-deps=true
